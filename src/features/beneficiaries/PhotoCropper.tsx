import { useCallback, useState } from 'react';
import Cropper, { type Area } from 'react-easy-crop';
import { Button } from '../../components/ui';
async function cropImage(src:string,area:Area){const image=new Image();image.src=src;await image.decode();const canvas=document.createElement('canvas');canvas.width=area.width;canvas.height=area.height;const ctx=canvas.getContext('2d')!;ctx.drawImage(image,area.x,area.y,area.width,area.height,0,0,area.width,area.height);return new Promise<Blob>((resolve,reject)=>canvas.toBlob(b=>b?resolve(b):reject(new Error('Falha ao recortar')),'image/jpeg',0.9));}
export function PhotoCropper({onChange}:{onChange:(blob:Blob)=>void}){const[src,setSrc]=useState('');const[crop,setCrop]=useState({x:0,y:0});const[zoom,setZoom]=useState(1);const[area,setArea]=useState<Area>();const select=(file?:File)=>{if(!file)return;if(!['image/jpeg','image/png','image/webp'].includes(file.type)||file.size>8*1024*1024){alert('Use JPG, PNG ou WEBP com até 8 MB.');return;}setSrc(URL.createObjectURL(file));};const save=useCallback(async()=>{if(src&&area){onChange(await cropImage(src,area));setSrc('');}},[src,area,onChange]);return <div className="space-y-3"><input type="file" accept="image/jpeg,image/png,image/webp" onChange={e=>select(e.target.files?.[0])}/>{src&&<><div className="relative h-80 overflow-hidden rounded-xl bg-slate-900"><Cropper image={src} crop={crop} zoom={zoom} aspect={3/4} onCropChange={setCrop} onZoomChange={setZoom} onCropComplete={(_,pixels)=>setArea(pixels)}/></div><input aria-label="Zoom" type="range" min="1" max="3" step="0.1" value={zoom} onChange={e=>setZoom(Number(e.target.value))}/><Button type="button" onClick={save}>Aplicar recorte 3x4</Button></>}</div>}
