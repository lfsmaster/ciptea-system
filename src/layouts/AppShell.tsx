import { useState, type ReactNode } from 'react';
import { NavLink } from 'react-router-dom';
import { Activity, BadgeCheck, FileCheck2, FileText, LayoutDashboard, LogOut, Menu, Settings, ShieldCheck, Users, X } from 'lucide-react';
import { useAuth } from '../features/auth/AuthProvider';
const items=[['/','Dashboard',LayoutDashboard],['/beneficiarios','Beneficiários',Users],['/solicitacoes','Solicitações',FileText],['/emissao','Emissão',BadgeCheck],['/templates','Templates',FileCheck2],['/usuarios','Usuários',ShieldCheck],['/auditoria','Auditoria',Activity],['/configuracoes','Configurações',Settings]] as const;
export function AppShell({children}:{children:ReactNode}){const [open,setOpen]=useState(false);const{profile,signOut}=useAuth();return <div className="min-h-screen bg-slate-50"><aside className={`fixed inset-y-0 left-0 z-40 w-72 transform bg-brand-900 text-white transition lg:translate-x-0 ${open?'translate-x-0':'-translate-x-full'}`}><div className="flex h-20 items-center justify-between border-b border-white/10 px-6"><div><div className="text-xl font-black">CIPTEA</div><div className="text-xs text-blue-200">Gestão e emissão</div></div><button className="lg:hidden" onClick={()=>setOpen(false)} aria-label="Fechar menu"><X/></button></div><nav className="space-y-1 p-4">{items.map(([to,label,Icon])=><NavLink key={to} to={to} end={to==='/'} onClick={()=>setOpen(false)} className={({isActive})=>`flex items-center gap-3 rounded-xl px-4 py-3 text-sm font-semibold ${isActive?'bg-white text-brand-900':'text-blue-100 hover:bg-white/10'}`}><Icon size={19}/>{label}</NavLink>)}</nav><div className="absolute bottom-0 left-0 right-0 border-t border-white/10 p-4"><div className="mb-3 px-2"><div className="truncate text-sm font-semibold">{profile?.full_name||'Usuário'}</div><div className="truncate text-xs text-blue-200">{profile?.roles?.join(', ')}</div></div><button onClick={()=>void signOut()} className="flex w-full items-center gap-3 rounded-xl px-4 py-3 text-sm font-semibold text-blue-100 hover:bg-white/10"><LogOut size={18}/>Sair</button></div></aside>{open&&<button className="fixed inset-0 z-30 bg-black/40 lg:hidden" aria-label="Fechar menu" onClick={()=>setOpen(false)}/>}<div className="lg:pl-72"><header className="sticky top-0 z-20 flex h-16 items-center border-b border-slate-200 bg-white px-4 lg:hidden"><button onClick={()=>setOpen(true)} aria-label="Abrir menu"><Menu/></button><span className="ml-3 font-bold text-brand-900">Sistema CIPTEA</span></header><main className="p-4 sm:p-6 lg:p-8">{children}</main></div></div>}
