import { Card,PageHeader } from '../components/ui';export function PlaceholderPage({title,description}:{title:string;description:string}){return <><PageHeader title={title} description={description}/><Card><p className="text-slate-600">O módulo está conectado à arquitetura, permissões e banco de dados. A interface detalhada será incrementada sem alterar o modelo de dados.</p></Card></>}
